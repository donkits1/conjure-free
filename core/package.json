{
  "name": "@conjure/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Conjure engine: supervisor, gateway, reconciler, providers, projections and the built-in check suite.",
  "license": "Apache-2.0",
  "bin": {
    "conjure": "./bin/conjure.mjs"
  },
  "scripts": {
    "check": "node dist/check/run.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "yaml": "^2.9.0"
  }
}
